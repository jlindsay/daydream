/**
 * author:Joshua Lindsay
 * email:j@lindsayfilm.com
 * http://lindsayfilm.com
 * Software can be used for Good or Evil.
 * Some people die rich, some die poor, the result is the same.
 */

function SearchManager()
{
/*
    var self = this;
    var _userid = '';
    var _config = {};
    var _user = {};
//    var _people = [];
//    var _characters = [];
//    var _movies = [];
//    var _shows = [];

    var results = [];
//    var _videos = [];


    function cons(key)
    {
        return { SEARCH_CONTROL_URL : "/search_control",
                 CONTROL_URL : "/control",
                 className:"MessageManager"
                }[key];
    }


    function init($userid, $config)
    {
        _userid = $userid;
        _config = $config || {};
    }

    function auth($userid, $config)
    {
        _userid = $userid;
        ///
    }

    function search( $userid, $keys $config)
    {
        console.log("debug:SearchManager:search(userid:"+$userid+",keys:"+$keys+")");
        $config = $config || {};
//        _type = $config.type || "search";


        $.ajax({
             url: cons("SEARCH_CONTROL_URL"),
              dataType: 'json',
            cache: false,
             data: {
                action : "search",
                userid : $userid,
                keys : $keys,
//                type : $type
            },
            success: function($data){
//                onCommentDeleted($data);
                try{ $config.success( $data );    }catch(e){};
            }, error: function($data)
            {
//                try{ $config.error( $data ); }catch(e){};
            }
        });

    }


    return {    auth : auth,
                search :search ,
                init : init

            }
*/

}
